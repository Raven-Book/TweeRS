---
tweers-cli: "patch:feat"
tweers-js: "patch:feat"
---

Integrate JavaScript hook scripts support in CLI. Scripts in `scripts/data/` and `scripts/html/` directories are now automatically detected and executed during the build pipeline at appropriate stages.
